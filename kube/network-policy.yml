apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  name: {{.FORM_API_NAME}}
spec:
  podSelector:
    matchLabels:
      name: {{.FORM_API_NAME}}
  egress:
  - {}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-external
    ports:
    - port: 10443
      protocol: TCP
