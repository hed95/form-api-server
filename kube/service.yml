---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{.FORM_API_NAME}}
  name: {{.FORM_API_NAME}}
spec:
  ports:
  - name: https
    port: 443
    targetPort: 10443
  selector:
    name: {{.FORM_API_NAME}}
