apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{.NAME}}-pod-tls
spec:
  secretName: {{.NAME}}-pod-tls
  issuerRef:
    name: platform-tls
    kind: ClusterIssuer
  commonName: {{.NAME}}.{{.KUBE_NAMESPACE}}.svc.cluster.local
  dnsNames:
  - localhost
  - 127.0.0.1
